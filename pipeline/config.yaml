parties: # parties default id
  guest:
    - 10000
  host:
    - 9997
    - 9998
    - 9999
  arbiter:
    - 6666

backend: 0 # 0 for eggroll, or 1 for spark

work_mode: 0 # 0 for standalone, or 1 for cluster

data_base_dir: "/data/projects/fate" # path to project base where data is located